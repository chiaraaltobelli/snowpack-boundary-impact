# Core numerics & arrays
numpy>=1.26

# Labeled arrays + lazy chunking
xarray>=2024.6.0
dask[array]>=2024.6.0

# NetCDF backend (xarray needs one of these; netCDF4 is the most common)
netCDF4>=1.6.5
cftime>=1.6.3

# Plotting
matplotlib>=3.8

# Mapping (Cartopy) and its runtime deps
cartopy>=0.23
shapely>=2.0
pyproj>=3.6

# Testing
pytest>=8.2

# Optional but often handy for analysis
pandas>=2.2
scipy>=1.11
